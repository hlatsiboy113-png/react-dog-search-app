# Place this file as ".env" in the PROJECT ROOT (same folder as package.json).
# Vite does NOT read src/.env
# No quotes, no spaces around =
# Restart npm run dev after saving.

VITE_DOG_API_KEY=your_api_key_here
